const simple = "hello world";
const singleQuote = "single quoted";
const withEscape = "line one\nline two";
const templateSimple = `template string`;
const templateInterp = `hello ${name}!`;
const templateMultiInterp = `${greeting}, ${name}!`;
const multilineTemplate = `line one
line two`;
const emptyString = "";
const emptyTemplate = ``;
const typeAnnotated: string = "typed string";
